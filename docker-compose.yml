version: '3.8'

services:

  backend:
    build: ./backend
    container_name: todo-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000

  frontend:
    build: ./frontend
    container_name: todo-frontend
    ports:
      - "8090:80"
    depends_on:
      - backend
